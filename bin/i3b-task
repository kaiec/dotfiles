#!/bin/bash
task=$(timew | grep "^Tracking .*" | sed "s/^Tracking //" | sed "s/\"//g" | sed "s/z:[^ ]*//")
time=$(timew | grep "^  Total" | sed "s/  Total[ ]*//")
if [[ $task ]]
then
	echo -n  $task \| $time
else
	echo ""
fi
