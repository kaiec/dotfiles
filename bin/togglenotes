#!/bin/bash
pgrep -f vim_notes
RUNNING=$?
if [ $RUNNING -eq 0 ]
then
	i3-msg \[instance=\"vim_notes\"\] scratchpad show
	exit 0
else
	urxvt -name vim_notes -e zsh -c "(cat ~/.cache/wal/sequences &);vim ~/Dropbox/Notizen/index.md" &
	sleep 1
	i3-msg \[instance=\"vim_notes\"\] scratchpad show 
fi
