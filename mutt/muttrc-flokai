source ~/dotfiles/mutt/config

# Automatically log in to this mailbox at startup
set spoolfile="~/Maildir/flokai/Inbox"
# Define the = shortcut, and the entry point for the folder browser (c?)
set folder="~/Maildir/flokai"

mailboxes +Inbox 

set record="=Inbox"
set postponed="=Drafts"
# set mbox="=archive"
set trash="=Trash"

set from="flo-kai@fam-eckert.net"
# set signature="~/dotfiles/hdm-signature.sig"

set sendmail="/home/kai/dotfiles/mutt/checkmail.py msmtpq -a flokai"
set sendmail_wait = -1

